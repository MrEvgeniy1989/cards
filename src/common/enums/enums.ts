export enum Route {
  Decks = '/decks',
  SignIn = '/login',
  SignUp = '/sign-up',
}

export enum ButtonVariant {
  Link = 'link',
  Primary = 'primary',
  Secondary = 'secondary',
  Tertiary = 'tertiary',
}
